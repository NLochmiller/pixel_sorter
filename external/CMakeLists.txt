if(NOT TARGET imgui)
  add_subdirectory(imgui imgui)
endif()
if(NOT TARGET file_browser)
  add_subdirectory(file_browser file_browser)
endif()
add_library(libs_main INTERFACE)
target_link_libraries(libs_main INTERFACE imgui file_browser)
